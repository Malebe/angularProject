import { NumCommandePipe } from './num-commande.pipe';

describe('NumCommandePipe', () => {
  it('create an instance', () => {
    const pipe = new NumCommandePipe();
    expect(pipe).toBeTruthy();
  });
});
